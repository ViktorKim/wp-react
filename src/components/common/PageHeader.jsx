import React from 'react';
import NavMenu from "./NavMenu";

const PageHeader = () => {
    return (
        <header>
            <div className="container">
                <NavMenu />
            </div>
        </header>
    )
};

export default PageHeader;